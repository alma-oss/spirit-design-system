import React from 'react';
import { CloseButton } from '..';

const CloseButtonDisabled = () => <CloseButton isDisabled UNSAFE_className="color-scheme-on-disabled" />;

export default CloseButtonDisabled;
