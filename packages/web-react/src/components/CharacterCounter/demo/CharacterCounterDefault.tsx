import React from 'react';
import CharacterCounter from '../CharacterCounter';

const CharacterCounterDefault = () => (
  <CharacterCounter id="character-counter-demo-default" counterThreshold={200} currentLength={0} />
);

export default CharacterCounterDefault;
