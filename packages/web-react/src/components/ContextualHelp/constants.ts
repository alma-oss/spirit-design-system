import { Placements, SizesExtended } from '../../constants';
import type { TooltipTriggerType } from '../../types';

export const DEFAULT_ICON_NAME = 'info';

export const DEFAULT_SIZE = SizesExtended.SMALL;

export const DEFAULT_PLACEMENT = Placements.TOP;

export const DEFAULT_TRIGGER: TooltipTriggerType[] = ['click', 'hover', 'focus'];
